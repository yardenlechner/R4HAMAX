name: REXX checks
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  rexx:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: sudo apt-get update && sudo apt-get install -y regina-rexx
      - run: python3 tests/run_checks.py regina
